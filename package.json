{
  "name": "particles-webgpu",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "WebGPU point cloud fractals",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node tests/browser-smoke.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "playwright": "1.63.0",
    "pngjs": "7.0.0"
  }
}
